apiVersion: v1
kind: ConfigMap
metadata:
  name: test-targets
data:
  api-endpoints.json: |
    {
      "baseUrl": "https://api-qa.example.com",
      "endpoints": [
        "/health",
        "/api/v1/users",
        "/api/v1/products"
      ]
    }
  ui-pages.json: |
    {
      "baseUrl": "https://app-qa.example.com",
      "pages": [
        "/login",
        "/dashboard",
        "/settings"
      ]
    }